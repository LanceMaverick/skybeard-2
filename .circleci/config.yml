version: 2
jobs:
  build:
    steps:
      - checkout
      - command: |
          python3 -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
      - run:
        . venv/bin/activate
        python main.py -k FAKEKEY -c config.yml.example --dry-run
          
      
